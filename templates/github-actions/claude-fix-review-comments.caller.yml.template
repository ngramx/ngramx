# Generated by: ngramx init-github-actions
# Calls reusable workflow: {{SHARED_REPO}} @ {{SHARED_REF}}
# Uses pull_request_target — review GitHub security guidance before enabling.

name: Claude Fix Review Comments

on:
  pull_request_target:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude-fix-review-comments:
    uses: {{SHARED_REPO}}/.github/workflows/claude-fix-review-comments.yml@{{SHARED_REF}}
    secrets: inherit
    with:
      php-version: '{{PHP_VERSION}}'
      node-version: '{{NODE_VERSION}}'
      run-composer-install: {{RUN_COMPOSER}}
      run-npm-ci: {{RUN_NPM}}
