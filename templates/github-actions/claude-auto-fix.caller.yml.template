# Generated by: ngramx init-github-actions
# Calls reusable workflow: {{SHARED_REPO}} @ {{SHARED_REF}}

name: Claude Auto-Fix CI

on:
  workflow_run:
    workflows: ["{{CI_WORKFLOW_NAME}}"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: read
  id-token: write

jobs:
  claude-auto-fix:
    uses: {{SHARED_REPO}}/.github/workflows/claude-auto-fix.yml@{{SHARED_REF}}
    secrets: inherit
    with:
      php-version: '{{PHP_VERSION}}'
      node-version: '{{NODE_VERSION}}'
      run-composer-install: {{RUN_COMPOSER}}
      run-npm-ci: {{RUN_NPM}}
