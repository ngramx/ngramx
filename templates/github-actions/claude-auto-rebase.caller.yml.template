# Generated by: ngramx init-github-actions
# Calls reusable workflow: {{SHARED_REPO}} @ {{SHARED_REF}}

name: Claude Auto-Rebase PRs

on:
  push:
    branches: ['{{BASE_BRANCH}}']
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude-auto-rebase:
    uses: {{SHARED_REPO}}/.github/workflows/claude-auto-rebase.yml@{{SHARED_REF}}
    secrets: inherit
    with:
      base-branch: '{{BASE_BRANCH}}'
      php-version: '{{PHP_VERSION}}'
      node-version: '{{NODE_VERSION}}'
      run-composer-install: {{RUN_COMPOSER}}
      run-npm-ci: {{RUN_NPM}}
